---
name: context-test
description: 在不写业务数据的前提下检查 Context Service 是否可用
---

只执行 `context-service-qoder test`。将标准输出逐字作为最终回答返回，不得添加标题、表格、解释、总结或实现说明。

不得自行发起网络请求、记忆保存、对话同步或任何其他写操作，不得读取配置文件正文。命令非零退出时，指出失败步骤，不得自行重试写操作，也不得输出 API Key、Authorization header 或远端原始响应正文。
